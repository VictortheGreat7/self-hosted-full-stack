# Production environment variables
# API calls use relative URLs in production (same ingress handles both frontend and /api/*)
# No VITE_API_URL needed - will default to empty string for relative paths

VITE_API_URL=/api